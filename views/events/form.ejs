<!DOCTYPE html>
<html>
<head>
	<title>Peak Seekers</title>
	<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
  <link rel="stylesheet" type="text/css" href="/stylesheet/main.css">
</head>
<body>
<nav class="navbar navbar-inverse">
  <div class="container">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <a class="navbar-brand" href="#">Peak Seekers</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><a href="/">Home<span class="sr-only">(current)</span></a></li>
        <li class="active"><a href="#">Blog</a></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if (!currentUser) { %>
          <li><a href="/login">Login</a></li>
          <li><a href="/register">Sign up</a></li> 
        <% } else { %>
          <li><a href="#">Signed In As <%= currentUser.username %></a></li>
          <li><a href="/logout">Logout</a></li>
        <% } %>
      </ul>
            
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
<div class="container-fluid">
	<div class="row" style="display: flex; flex-direction: row;">
    <div id="info" class="col-lg-6 col-md-6">
      <div class="thumbnail">
          <img class="img-responsive" src="<%= data.image%>">
          <div class="caption-full" >
            <h4 class="pull-right"><%= data.price %> RS/Night</h4>
            <h3><a><%= data.name %></a></h3>
            <p><%= data.description %></p>
            <hr> 
            <p>Going on date: <%= data.date.toDateString() %></p>
            <p><b>Things to Keep in mind</b></p>
            <ul>
              <li>Identity Proof</li>
              <li>Backpack and an extra bag</li>
              <li>Sufficient pair of clothes and necessary items</li>
              <li>Trekking shoes are must</li>
              <li>Torch, batteries, plastic bag, glasses, cap, scarf, gloves, socks, jacket, etc</li>
              <li>Eatables as required and 2 water bottles of 1 ltr is must</li>
            </ul> 
          </div>
      </div>
    </div>
    
    <div id="form" class="col-lg-6 col-md-6" style="width:30%; margin:30px auto;">
      <form action="/events/book/<%=data._id%>" action="/admin/history" method="POST">
        <div class="form-group">
            <input type="text" class="form-control" name="name" value="<%= currentUser.username %>" required placeholder="name">
        </div>
        <div class="form-group">
            <input type="text" class="form-control" name="contact" placeholder="contact no" required>
        </div>
        <div class="form-group">
            <input type="text" class="form-control" name="email" placeholder="email id" required 
            pattern="[a-z0-9._%+-]+@[a-z0-9.-]+\.[a-z]{2,}$" title="Submit valid email id">
        </div>
        <div class="form-group">
          <textarea class="form-control" rows="2" name="address" placeholder="address" required></textarea>
        </div>

        <div class="form-group">
            <input type="number" class="form-control" name="people" placeholder="people" min="1" max="10" required="Please choose number of people coming">
        </div>

        <div class="form-group">
        <button class="btn btn-success btn-block">Confirm</button>        
        </div>
      </form>
    </div>
	</div>
</div>	
</body>
</html>